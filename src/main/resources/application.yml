#server config
server:
  port: 8080
  servlet:
    context-path: /video

spring:
  #thymeleaf config
  thymeleaf:
    mode: HTML5
    encoding: UTF-8
    servlet:
      content-type: text/html
    cache: false
  #datasource config
  datasource:
    name: test
    url: jdbc:mysql://127.0.0.1:3306/rcs
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

